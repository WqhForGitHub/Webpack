// src/index.js
// 引入第三方库（lodash），用以放大 babel-loader 对 node_modules 的处理开销

import _ from "lodash";

const arr = [1, 2, 3, 4, 5];
console.log("sum =", _.sum(arr));
console.log("chunk =", _.chunk(arr, 2));
